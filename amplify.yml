version: 1
frontend:
  phases:
    build:
      commands:
        - cd backend
        - pip install -r requirements.txt
        - cd ..
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths:
      - ~/.cache/pip/**/*
